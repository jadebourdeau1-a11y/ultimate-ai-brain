import datetime
from self_improvement import log_trade

def execute_trade(ticker, action, quantity, price):
    if action not in ["BUY", "SELL"]:
        return False
    log_trade(ticker, action, quantity, price)
    print(f"{datetime.datetime.now()} - Executed {action} {quantity} of {ticker} at {price}")
    return True
